[workspace]
members = [
    "webizen-desktop",
    "webizen-runtime",
    "webizen-studio",
    "webizen-render"
]
resolver = "2"

[profile.release]
opt-level = 'z'
lto = true
codegen-units = 1
panic = "abort"
